# bf-518l — resolve_real_binary self-recursion-guard test (retry)

This bead was re-dispatched as a **retry** (`failure-count:1`). The code it
asked for was already complete from a prior attempt, so this run produced no
source changes — it only verified the work and closed the bead.

## What the bead asked for (src/shim.rs, `#[cfg(test)] mod tests`)

Test (b), the self-recursion guard: with `PATH` reduced to ONLY `shim_dir()`
and no override (`Config::hardcoded()` → `real_binary == None`),
`resolve_real_binary` strips that dir (it excludes `current_exe`'s dir via
`strip_dir_from_path`), the search path collapses to empty, and the `cargo`
walk returns `Err` — the resolver never re-resolves `cargo` to the gantry
binary itself.

## State found on entry

- The target test,
  `resolve_real_binary_self_recursion_guard_yields_err_when_path_is_only_shim_dir`,
  was already present (commit `28c5025`, "Add resolve_real_binary
  self-recursion-guard test (shim.rs)") and pushed.
- The checkpoint commit `d4c67ee` had recorded `bf-518l` as `closed` in
  `.beads/issues.jsonl`.
- But the live `beads.db` showed `bf-518l` as `in_progress` (db/JSONL
  divergence after the bead was re-opened for retry) — matching the
  [[auto-split-shared-branch-push-divergence]] pattern, where bf-2cuz's
  children share one wip branch and the bead state drifts between the db and
  the checkpointed JSONL across attempts.

## Verification this attempt (all green)

- `cargo fmt --check` — clean.
- `cargo clippy --all-targets -- -D warnings` — no warnings.
- `cargo test` — 33 passed, 0 failed; the target test passes: a PATH of only
  the shim dir yields `Err` whose message names `cargo`, never the gantry
  binary. Uses `with_path` (serialized behind `ENV_LOCK`, PATH restored on
  drop) so no synthetic PATH leaks into sibling tests.

## Outcome

Code unchanged and already committed/pushed; bead closed via `br close bf-518l`.
